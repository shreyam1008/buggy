# Static files — serve as-is (must come BEFORE the SPA catch-all)
/sitemap.xml   /sitemap.xml   200
/robots.txt    /robots.txt    200

# SPA fallback — Cloudflare Pages serves index.html for all client-side routes
/*  /index.html  200
